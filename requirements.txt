# --- Web framework -----------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
gunicorn>=23.0,<24.0
python-multipart>=0.0.12,<1.0

# --- Data / validation -------------------------------------------------------
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
email-validator>=2.2,<3.0    # backs pydantic's EmailStr

# --- Database ----------------------------------------------------------------
SQLAlchemy[asyncio]>=2.0.36,<2.1
alembic>=1.14,<2.0
asyncpg>=0.30,<0.31          # PostgreSQL driver (production / Render)
aiosqlite>=0.20,<1.0         # SQLite driver (local development)
greenlet>=3.1,<4.0

# --- Security ----------------------------------------------------------------
bcrypt>=4.2,<5.0
PyJWT>=2.9,<3.0

# --- Machine learning --------------------------------------------------------
scikit-learn>=1.5,<2.0
numpy>=1.26,<3.0
scipy>=1.13,<2.0
pandas>=2.2,<3.0
joblib>=1.4,<2.0

# --- Optional LLM engine (degrades gracefully when the key is absent) --------
anthropic>=0.40,<1.0

# --- Observability -----------------------------------------------------------
structlog>=24.4,<26.0

# --- HTTP client -------------------------------------------------------------
httpx>=0.27,<1.0
